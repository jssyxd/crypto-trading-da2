# ============================================================================
# Lighter 现货交易专用依赖包列表（新版 SDK 1.0.1+）
# ============================================================================
#
# 📌 使用场景：
#   - Lighter 现货网格交易系统
#   - 使用新版 lighter-sdk 1.0.1+
#
# 📌 虚拟环境安装：
#   python3.12 -m venv .venv-lighter-spot
#   source .venv-lighter-spot/bin/activate
#   pip install -r requirements-lighter-spot.txt
#
# ⚠️ 重要说明：
#   - 此依赖文件与 requirements-py312.txt 存在冲突（eth-account 版本不兼容）
#   - 不要在同一个虚拟环境中同时安装两者
#   - 旧版脚本（如套利系统）应使用独立的虚拟环境
#
# ============================================================================

# ────────────────────────────────────────────────────────────────────────────
# 🏛️ 核心框架 (Core Framework)
# ────────────────────────────────────────────────────────────────────────────
fastapi==0.104.1
uvicorn==0.24.0
pydantic>=2.5.0
pydantic-settings==2.1.0
injector==0.21.0

# ────────────────────────────────────────────────────────────────────────────
# 🔄 异步和网络 (Async & Networking)
# ────────────────────────────────────────────────────────────────────────────
aiohttp>=3.9.0
aiofiles>=23.0.0
websockets>=12.0
websocket-client>=1.6.0
tenacity>=8.2.3

# ────────────────────────────────────────────────────────────────────────────
# 🔗 Lighter 交易所 SDK（新版本）
# ────────────────────────────────────────────────────────────────────────────

# 🔥 Lighter 官方 SDK 1.0.1+（PyPI 版本）
lighter-sdk==1.0.1

# 🔥 新版 SDK 所需的加密依赖（高版本）
# 注意：这些版本与 paradex-py 不兼容
eth-account>=0.12.0,<0.14.0  # 新版 SDK 需要 >=0.12.0
hexbytes>=1.2.0,<2.0.0       # 新版 SDK 需要 >=1.2.0
ckzg>=1.0.0                  # 新版 SDK 需要 >=1.0.0 (eth-account>=0.13.4 需要 >=2.0.0)

# 以太坊相关依赖
eth-typing>=3.5.0,<4.0.0
eth-utils>=2.3.0,<3.0.0
eth-abi>=4.0.0,<5.0.0
eth-keys>=0.4.0,<0.5.0
rlp>=3.0.0,<4.0.0

# Web3 依赖（如果需要）
web3>=6.0.0,<7.0.0

# 加密相关
cytoolz>=0.12.0
pycryptodome>=3.19.0

# ────────────────────────────────────────────────────────────────────────────
# 🔗 其他交易所适配器（可选）
# ────────────────────────────────────────────────────────────────────────────

# CCXT（通用交易所库）
ccxt>=4.4.30

# ⚠️ 注意：以下 SDK 与新版 lighter-sdk 存在依赖冲突，不建议同时安装
# paradex-py  # 需要 eth-account<0.12.0，与 lighter-sdk 1.0.1 冲突

# EdgeX SDK（如果需要）
# git+https://github.com/edgex-Tech/edgex-python-sdk.git

# Hyperliquid SDK（如果需要）
# hyperliquid-python-sdk
# httpx>=0.25.0

# Backpack 签名依赖（如果需要）
# PyNaCl>=1.5.0

# ────────────────────────────────────────────────────────────────────────────
# 📊 数据处理 (Data Processing)
# ────────────────────────────────────────────────────────────────────────────
pandas>=2.1.0
numpy>=1.26.0  # Python 3.12 需要 numpy >= 1.26
plotly>=5.18.0  # 图表生成（历史记录功能需要）
streamlit>=1.28.0  # 实时监控仪表板（历史记录可视化功能需要）

# ────────────────────────────────────────────────────────────────────────────
# 🖥️ 终端 UI (Terminal UI)
# ────────────────────────────────────────────────────────────────────────────
rich==13.7.0
psutil==5.9.6

# ────────────────────────────────────────────────────────────────────────────
# ⚙️ 配置管理 (Configuration)
# ────────────────────────────────────────────────────────────────────────────
pyyaml==6.0.1
python-dotenv==1.0.0

# ────────────────────────────────────────────────────────────────────────────
# 🗄️ 基础设施 (Infrastructure) - 可选
# ────────────────────────────────────────────────────────────────────────────
# redis==5.0.1
# sqlalchemy==2.0.23
# alembic==1.13.1
# aiosqlite>=0.19.0

# ────────────────────────────────────────────────────────────────────────────
# 🧪 测试框架 (Testing) - 可选
# ────────────────────────────────────────────────────────────────────────────
pytest==7.4.3
pytest-asyncio==0.21.1
# pytest-cov==4.1.0

# ────────────────────────────────────────────────────────────────────────────
# 📝 版本验证
# ────────────────────────────────────────────────────────────────────────────
# 安装完成后，验证版本：
#   python -c "import lighter; print(f'Lighter SDK: {lighter.__version__}')"
#   python -c "import eth_account; print(f'eth-account: {eth_account.__version__}')"
#   python -c "import hexbytes; print(f'hexbytes: {hexbytes.__version__}')"
#
# 预期输出：
#   Lighter SDK: 1.0.1
#   eth-account: 0.12.x 或 0.13.x
#   hexbytes: 1.2.x 或更高
# ────────────────────────────────────────────────────────────────────────────

