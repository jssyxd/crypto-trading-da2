# ============================================================================
# 套利监控系统配置 V1
# ============================================================================
# 
# 所属系统: 套利监控系统 V1（旧版本）
# 使用脚本: run_arbitrage_monitor.py
# 模块路径: core/services/arbitrage_monitor/
# ============================================================================

# 交易所配置（当前专注开发 Lighter、EdgeX、Paradex）
exchanges:
  # - backpack  # 暂时禁用，等脚本稳定后再接入
  - edgex
  - lighter
  - paradex

# 监控的交易对（标准化符号格式）
# 🚀 47个高交易量代币（交易量 >= 1M USD，从 edgex_lighter_markets.json 筛选）
symbols:
  - 1000PEPE-USDC-PERP
  - 1000SHIB-USDC-PERP
  - AAVE-USDC-PERP
  - ADA-USDC-PERP
  - APT-USDC-PERP
  - ARB-USDC-PERP
  - ASTER-USDC-PERP
  - AVAX-USDC-PERP
  - AVNT-USDC-PERP
  - BCH-USDC-PERP
  - BNB-USDC-PERP
  - BTC-USDC-PERP
  - CRO-USDC-PERP
  - DOGE-USDC-PERP
  - DOT-USDC-PERP
  - ENA-USDC-PERP
  - ETH-USDC-PERP
  - FIL-USDC-PERP
  - HBAR-USDC-PERP
  - HYPE-USDC-PERP
  - ICP-USDC-PERP
  - IP-USDC-PERP
  - KAITO-USDC-PERP
  - LINK-USDC-PERP
  - LTC-USDC-PERP
  - MNT-USDC-PERP
  - NEAR-USDC-PERP
  - ONDO-USDC-PERP
  - OP-USDC-PERP
  - PAXG-USDC-PERP
  - PUMP-USDC-PERP
  - RESOLV-USDC-PERP
  - SEI-USDC-PERP
  - SOL-USDC-PERP
  - SUI-USDC-PERP
  - TAO-USDC-PERP
  - TON-USDC-PERP
  - TRUMP-USDC-PERP
  - TRX-USDC-PERP
  - UNI-USDC-PERP
  - VIRTUAL-USDC-PERP
  - WLD-USDC-PERP
  - XMR-USDC-PERP
  - XRP-USDC-PERP
  - ZEC-USDC-PERP
  - ZK-USDC-PERP
  - ZORA-USDC-PERP

# 套利阈值配置
thresholds:
  price_spread: 0.005        # 价差阈值（0.5%）
  funding_rate: 0.0001       # 资金费率差阈值（0.01%）
  min_score: 0.001           # 最小评分阈值（0.1%）

# 监控配置
monitoring:
  update_interval: 5         # 更新间隔（秒）
  enable_sound_alert: true   # 启用声音报警
  alert_repeat: 3            # 报警重复次数

# 终端UI配置
display:
  refresh_rate: 1.0          # 刷新率（秒）
  max_opportunities: 200     # 最多显示的套利机会数量（增加到200）
  show_all_prices: true      # 显示所有交易对价格
  show_funding_rates: true   # 显示资金费率

# 日志配置
logging:
  level: INFO
  file: logs/arbitrage_monitor.log
  console: true

# 调试CLI输出（重构版统一调度器）
debug_cli:
  enabled: true              # 设为true启用纯CLI调试模式
  interval_seconds: 1.0      # CLI状态打印间隔（秒）

