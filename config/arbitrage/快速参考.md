# 套利系统配置快速参考

## 🎯 我想做什么？

### 1️⃣ Lighter 对多个交易所套利（1对多 ）

**修改配置**：
```yaml
# multi_exchange_arbitrage.yaml
enabled: true
mode: "one_to_many"
center_exchange: "lighter"
counter_exchanges: ["edgex", "backpack", "paradex"]
symbols: ["BTC-USDC-PERP"]

# arbitrage_segmented.yaml
system_mode:
  monitor_only: false  # 实盘
symbol_configs:
  "BTC-USDC-PERP":
    quantity_config:
      base_quantity: 0.001
```

**运行命令**：
```bash
python main_unified.py \
  --config config/arbitrage/arbitrage_segmented.yaml \
  --monitor-config config/arbitrage/monitor_v2.yaml
```

---

### 2️⃣ 只做 Paradex ↔ Lighter 套利（1对1）

**修改配置**：
```yaml
# multi_exchange_arbitrage.yaml
enabled: false  # 关闭1对多

# arbitrage_segmented.yaml
system_mode:
  monitor_only: false
symbol_configs:
  "BTC-USDC-PERP":
    quantity_config:
      base_quantity: 0.001
```

**运行命令**：
```bash
python main_unified.py \
  --config config/arbitrage/arbitrage_segmented.yaml \
  --monitor-config config/arbitrage/monitor_paradex_lighter_btc.yaml
```

---

### 3️⃣ Lighter 黄金套利（PAXG ↔ XAU）

**修改配置**：
```yaml
# multi_leg_pairs.yaml
enabled: true
pairs:
  - pair_id: "LIGHTER_PAXG_XAU"
    exchange: "lighter"
    legs:
      - symbol: "PAXG-USD-PERP"
        direction: "buy"
      - symbol: "XAU-USD-PERP"
        direction: "sell"

# arbitrage_segmented.yaml
symbol_configs:
  "LIGHTER_PAXG_XAU":
    quantity_config:
      base_quantity: 0.006
```

**运行命令**：
```bash
python main_unified.py \
  --config config/arbitrage/arbitrage_segmented.yaml \
  --monitor-config config/arbitrage/monitor_lighter_gold.yaml
```

---

### 4️⃣ 只监控价差，不下单

**修改配置**：
```yaml
# arbitrage_segmented.yaml
system_mode:
  monitor_only: true  # 仅监控
```

**运行命令**（任选其一）：
```bash
# 方式1：使用执行脚本
python main_unified.py \
  --config config/arbitrage/arbitrage_segmented.yaml \
  --monitor-config config/arbitrage/monitor_v2.yaml

# 方式2：使用纯监控脚本
python run_arbitrage_monitor_v2.py \
  --config config/arbitrage/monitor_v2.yaml
```

---

## 📋 配置文件速查表

| 配置文件 | 作用 | 何时修改 |
|---------|------|---------|
| `arbitrage_segmented.yaml` | 交易参数、数量、风控 | ✅ 每次都要检查 |
| `multi_exchange_arbitrage.yaml` | 1对多套利规则 | ✅ 做1对多时修改 |
| `multi_leg_pairs.yaml` | 多腿套利规则 | ✅ 做多腿时修改 |
| `monitor_v2.yaml` | 通用监控配置 | ⚠️ 一般不改 |
| `monitor_paradex_lighter_btc.yaml` | Paradex↔Lighter专用 | ⚠️ 场景专用 |
| `monitor_lighter_gold.yaml` | 黄金套利专用 | ⚠️ 场景专用 |

---

## 🔧 关键配置项

### 实盘 vs 监控
```yaml
# arbitrage_segmented.yaml
system_mode:
  monitor_only: false  # false=实盘，true=监控
```

### 触发价差
```yaml
# arbitrage_segmented.yaml
symbol_configs:
  "BTC-USDC-PERP":
    grid_config:
      initial_spread_threshold: 0.1  # 0.1% 触发
```

### 下单数量
```yaml
# arbitrage_segmented.yaml
symbol_configs:
  "BTC-USDC-PERP":
    quantity_config:
      base_quantity: 0.001  # 每格数量
```

### 启用1对多
```yaml
# multi_exchange_arbitrage.yaml
enabled: true  # true=启用，false=禁用
```

### 启用多腿
```yaml
# multi_leg_pairs.yaml
enabled: true  # true=启用，false=禁用
```

---

## ⚠️ 运行前检查

- [ ] API 密钥已配置（`.env` 文件）
- [ ] `monitor_only` 设置正确
- [ ] 下单数量符合精度要求
- [ ] 风控参数合理
- [ ] 套利规则已启用（`enabled: true`）

---

## 🆘 遇到问题？

1. 查看日志：`logs/unified_orchestrator.log`
2. 检查配置：确保 YAML 格式正确
3. 验证连接：确保所有交易所 WebSocket 连接正常
4. 查看详细文档：`README_配置文件说明.md`

